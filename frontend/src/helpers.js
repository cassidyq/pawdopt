
import Cookies from 'universal-cookie';

// const cookies = new Cookies();


const addToFavourites = function (animal_id) {
  console.log('animal ID: ', animal_id)
}
module.exports = {
  addToFavourites
}